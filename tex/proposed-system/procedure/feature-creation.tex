\begin{figure}[t]
  \centerline{\includegraphics[width=\linewidth]{img/3d.png}}
  \caption{Projection of 3D coordinates}
  \label{trans3dto2d}
\end{figure}

\deleted{We carry out image-based feature modeling as the first method.
This method generates image arrays from frames of time-series point-cloud data and converts them into statistics of similarity changes between the image arrays.
The hypothesis of this method is that changes in a person's overall physical movement affect activity levels.
Such changes can be measured as changes in the similarity of point-cloud frames \cite{kizawa2023estimation}.

The method follows a specific process for feature modeling.
We convert 3D point-cloud data into two-dimensional (2D) information for each frame.
We project each point in the point-cloud data onto a plane formed by two of the three coordinates ($x$, $y$, and $z$), as shown in Fig.~\ref{trans3dto2d}.
We carry out the projection on all three axes for all frames.
We then convert a 3D point-cloud frame into a 2D image and generate a time-series 2D pixel array.
We refer to the time-series 2D pixel array as a `video frame' going forward.
We calculate a similarity between frames of the video frame and use zero-mean normalized cross-correlation (ZNCC) as a measure of image similarity, which is commonly used for measuring image similarity, particularly in template matching \cite{chen2020self,NAKHMANI2013315}.}
\revised{Image-based feature modeling generates image arrays from time-series point-cloud frames and converts similarity changes between the arrays into statistics \cite{kizawa2023estimation}.
Overall physical movement is assumed to affect activity levels through changes in frame similarity.
For each frame, 3D point-cloud data are projected onto planes formed by pairs of the three coordinates ($x$, $y$, and $z$), as shown in Fig.~\ref{trans3dto2d}.
The projection is applied on all three axes for all frames.
Each 3D frame is converted into a 2D image to form a time-series 2D pixel array referred to as a `video frame'.
Frame-to-frame similarity is measured using zero-mean normalized cross-correlation (ZNCC) \cite{chen2020self,NAKHMANI2013315}.}
\deleted{ZNCC is defined as:
\begin{equation}
  \label{eq:zncc}
  \mathrm{ZNCC} = \frac{\sum_{i=1}^{MN} (x_{i}-\bar{x})(y_{i}-\bar{y})}{\sqrt{\sum_{i=1}^{MN} (x_{i}-\bar{x})^2 \sum_{i=1}^{MN} (y_{i}-\bar{y})^2}},
\end{equation}
where $x$ and $y$ are the intensity values of the 1st and 2nd dimensions, and $M$ and $N$ are the number of pixels of 1st and 2nd dimensions, respectively.}
\revised{ZNCC is computed between consecutive frames for the 1st to $I-1$-th frames, where $I$ is the number of frames.
The 50th and 90th percentile values of the ZNCC distribution are extracted as numerical features.
\revised{The 50th percentile value reflects the central tendency of the ZNCC distribution with minimal outlier influence.
The 90th percentile value captures upper-range similarity changes not represented by the central tendency alone.}

Centroid feature modeling converts a head region of time-series point-cloud data into statistics of center-of-gravity changes.
Head movement is assumed to affect activity levels through center-of-gravity changes.
A head region is defined from 3D coordinates in an initial frame and extracted from all frames.
The sum of each coordinate in the head region is calculated for each frame.}
Each frame is represented as a set of $n$ points ($x_{i}$, $y_{i}$, $z_{i}$) in 3D coordinate space.
\deleted{The sum of the $x$, $y$, and $z$ coordinates is defined as
\begin{equation}
  \left\{
  \begin{aligned}
    X &= \sum_{i=1}^{n} x_{i},\\
    Y &= \sum_{i=1}^{n} y_{i},\\
    Z &= \sum_{i=1}^{n} z_{i}.
  \end{aligned}
  \right.
\end{equation}
The center of gravity $G_{j}$ for each frame up to $I$ is expressed as the sum of each coordinate divided by the total number of points $n$:
\begin{equation}
  G_{j} = \left(\frac{X}{n}, \frac{Y}{n}, \frac{Z}{n}\right),
\end{equation}
where $j$ represents the frame number, ranging from the 1st frame to the $I$-th frame.
The difference between the central point $C$ of the center of gravity of all frames and the center of gravity of each frame is calculated.
The central point $C$ is defined as
\begin{equation}
  C = \frac{1}{I}\sum_{j=1}^{I} G_{j}.
\end{equation}
The difference is represented as Euclidean distance \cite{blais1995registering}.}
\revised{The coordinate sums are defined as $X=\sum_{i=1}^{n}x_{i}$, $Y=\sum_{i=1}^{n}y_{i}$, and $Z=\sum_{i=1}^{n}z_{i}$.
The center of gravity $G_{j}$ for each frame up to $I$ is $G_{j}=(X/n, Y/n, Z/n)$, where $j$ is the frame number from the 1st to the $I$-th frame.
The central point $C$ of the center of gravity of all frames is $C=\frac{1}{I}\sum_{j=1}^{I}G_{j}$.
The difference between $C$ and $G_{j}$ is represented as Euclidean distance \cite{blais1995registering}.
The 50th and 90th percentile values of the center-of-gravity difference distribution are extracted as numerical features.}
