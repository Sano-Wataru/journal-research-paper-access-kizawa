We first acquire 3D stream data then convert the acquired 3D stream data into point-cloud data, represented as a set of points with 3D coordinates.
We integrate the point-cloud data from each device on the basis of timestamps and coordinates.