We estimate activity levels using machine-learning models.
We handle multiple feature inputs with the models.
The machine-learning models take feature-modeled point-cloud data as input and use labels assigned on the basis of activity levels as output.
The models should be capable of handling multivariate inputs.
We extract the importance of each feature in the estimation process and show the impact of each feature importance on activity levels.
