Table \ref{matrix_rf_test} shows the confusion matrices of RF models, four with a single feature and one with all features. 
% 
Table \ref{matrix_rf_test_a} shows the confusion matrix of the model with image-based 50th percentile values as input features. 
% 
The model showed a trend toward lower FPR and higher FNR.
% 
Table \ref{matrix_rf_test_b} shows the confusion matrix of the model with image-based 90th percentile values as input features. 
% 
The model showed a trend toward lower FNR and higher FPR.
%
Table \ref{matrix_rf_test_c} shows the confusion matrix of the model with centroid 50th percentile values as input features. 
% 
The model showed a trend of slightly higher FNR and lower FPR.
%
Table \ref{matrix_rf_test_d} shows the confusion matrix of the model with centroid 90th percentile values as input features. 
% 
The model showed a trend toward lower FNR and higher FPR.
% 
Table \ref{matrix_rf_test_e} shows the confusion matrix of the model with all feature values as input. 
% 
The model showed a trend toward lower FNR and higher FPR.
 
%
Table \ref{result_rf_test} shows the values of the evaluation index for each RF model.
% 
The model with the lowest FNR was the one using image-based 90th percentile values.
%
The model with the lowest FPR was the one that used both centroid 50th and 90th percentile values.
% 
The model with the highest accuracy was the one that used both centroid 50th and 90th percentile values.
